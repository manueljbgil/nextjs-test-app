import styled from 'styled-components'

export const Title = styled.h5`
    text-transform: uppercase;
    text-align: center;
    font-weight: bold;
    font-size: 24px;
    color: col.$primary;
    line-height: 1.4em;
`
